stages:
    - build

build:
    tags:
        - SE140-2026
    stage: build
    image:
        name: alpine:latest
    script:
        - npm ci
        - npx tsc log/logService.ts --outDir dist --module nodenext --target esnext
    artifacts:
        name: log-service-artifact
        paths:
            - dist/
